use Test;
use Samaki::Plugin::Process;

class TestProcess does Samaki::Plugin::Process[name => 'test', cmd => 'echo'] { }

my $plugin = TestProcess.new;

my @cmd = $plugin.command;
ok @cmd.elems > 0, 'command returns a list';

done-testing;
